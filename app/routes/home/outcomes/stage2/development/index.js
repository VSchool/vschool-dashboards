import DisplayData from '../../../../components/displayData';

export default function AllStage2Page() {
    return (
        <main>
            <DisplayData page={'stage2'} dept={'outcomes'} course={'FSJS'} title={'Stage 2 Development Records'} />
        </main>
    );
}