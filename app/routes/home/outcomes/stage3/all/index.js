import DisplayData from "../../../../components/displayData";

export default function AllStage3Page() {
    return (
        <main>
            <DisplayData title={'All Stage 3 Records'} dept={'outcomes'} page={'stage3'} />
        </main>
    );
}