import DisplayData from '../../../../components/displayData';

export default function AllStage3Page() {
    return (
        <main>
            <DisplayData title={'Stage 3 Development Records'} page={'stage3'} dept={'outcomes'} course={'FSJS'} />
        </main>
    );
}